export enum ButtonType {
  Button = 'button',
  Submit = 'submit',
  Reset = 'reset'
}

export enum ButtonSize {
  Xs = 'xs',
  XsCompact = 'xs-compact',
  S = 's',
  SCompact = 's-compact',
  M = 'm',
  MCompact = 'm-compact',
  L = 'l',
  LCompact = 'l-compact'
}

export enum ButtonModifiers {
  Invert = 'invert',
  Outline = 'outline',
  Link = 'link',
  Flat = 'flat'
}
