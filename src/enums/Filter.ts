export enum FilterRelations {
  Exists = 'exists',
  Is = 'is',
  IsNot = 'is_not'
}

export enum FilterTypes {
  Relation = 'relation',
  Date = 'date',
  Text = 'text',
  Range = 'range',
  Fast = 'fast'
}
